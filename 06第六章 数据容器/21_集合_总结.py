"""第二十一节，集合_总结"""

"""
本节对应课程大纲里的知识点：集合特点总结

┌──────┬──────────────────────────────────────────────┐
│ 特点  │ 说明                                         │
├──────┼──────────────────────────────────────────────┤
│ 无序  │ 元素没有固定顺序，无法通过下标访问                │
│ 不重复 │ 自动去重，同一个元素只会保留一份                  │
│ 分两种 │ 可变集合（set）和不可变集合（frozenset）         │
│ 元素限制│ 元素必须是不可变类型（数字、字符串、元组）         │
│ 数学运算│ 支持并集 |、交集 &、差集 -、对称差集 ^          │
└──────┴──────────────────────────────────────────────┘

一句话总结：集合是可以去重的数据容器，
当只关心元素是否存在，而不在乎顺序时，首选集合。
"""

# 本节是纯总结，无代码练习
