"""第二十二节，字典_定义字典"""

"""
这一节代码整体在讲：字典是什么，以及如何定义字典。

本节对应课程大纲里的知识点：
1. 什么是字典——键值对结构
2. 定义字典的语法
3. 定义空字典
4. key 不能重复
5. key 必须是不可变类型
6. value 可以是任意类型
7. 字典嵌套

先记一句最重要的话：
- 字典就像一个带标签的收纳盒，贴上标签（键），放进东西（值）
"""

print('============================ 教程区 ============================')

print('------------------------- 1. 定义字典 --------------------------')
# 字典（dict）用来存放"键值对"，每个元素格式为 key:value（key 在前，value 在后）
# 通过键（key）找值（value），列表用下标找值，字典用键找值

stu = {'姓名': '张三', '年龄': 18, '成绩': 85}
print(type(stu))  # 输出：<class 'dict'>
print(stu)  # 可能输出：{'姓名': '张三', '年龄': 18, '成绩': 85}
print()

# 空字典有两种定义方式：{} 和 dict()
# 注意 {} 是空字典，不是空集合
empty1 = {}
empty2 = dict()
print(type(empty1), empty1)  # 输出：<class 'dict'> {}
print(type(empty2), empty2)  # 输出：<class 'dict'> {}
print()

print('------------------------ 2. key 的特性 ------------------------')
# key 不能重复，后写的会覆盖前写的

repeat = {'张三': 72, '李四': 60, '王五': 85, '张三': 99}
print(repeat)  # 可能输出：{'张三': 99, '李四': 60, '王五': 85}

# key 必须是不可变类型（数字、字符串、元组），列表等可变类型不行

valid_keys = {250: '数字', 'name': '字符串', (1, 2): '元组'}
print(valid_keys)  # 可能输出：{250: '数字', 'name': '字符串', (1, 2): '元组'}
print()

print('----------------------- 3. value 的特性 -----------------------')
# value 可以是任意类型：数字、字符串、列表、布尔值……

mix = {'姓名': '张三', '年龄': 18, '成绩': 72.5, '爱好': ['抽烟', '喝酒', '烫头'], '是否在校': True}
print(type(mix))  # 输出：<class 'dict'>
print(mix)  # 可能输出：{'姓名': '张三', '年龄': 18, '成绩': 72.5, '爱好': ['抽烟', '喝酒', '烫头'], '是否在校': True}
print()

print('------------------------- 4. 字典嵌套 --------------------------')
# 字典的值也可以是字典，这叫嵌套，适合存有结构的数据

nested = {
    1001: {'name': '张三', 'score': 85},
    1002: {'name': '李四', 'score': 92}
}
print(type(nested))  # 输出：<class 'dict'>
print(nested)  # 可能输出：{1001: {'name': '张三', 'score': 85}, 1002: {'name': '李四', 'score': 92}}
print()

print('============================ 小结 ============================')
# 1. 字典用 {} 定义，元素格式为 key:value
# 2. 空字典可以用 {} 或 dict() 创建
# 3. key 不能重复，重复后覆盖
# 4. key 必须是不可变类型（数字/字符串/元组）
# 5. value 可以是任意类型
# 6. 字典可以嵌套，值里再放字典

print('=========================== 习题区 ============================')


print('--------------------------- 练习 1 ---------------------------')
# 练习 1：
# 请定义一个字典，表示你的个人信息，包含：
# name（姓名）、age（年龄）、city（城市）
information = {'name':'周宇祥','age':23,'city':'郑州'}
print(information)

print('--------------------------- 练习 2 ---------------------------')
# 练习 2：
# 请创建一个空字典，然后直接打印它
print({})
print(dict())

print('--------------------------- 练习 3 ---------------------------')
# 练习 3：
# 有如下字典：
# scores = {'语文': 90, '数学': 95, '英语': 88}
# 请在字典中再添加一个 '语文': 100，观察打印结果中 '语文' 的值是多少
scores = {'语文': 90, '数学': 95, '英语': 88,'语文': 100}
print('语文的值是',scores,'后面的相同key的value值覆盖前面的value')

print('--------------------------- 练习 4 ---------------------------')
# 练习 4：
# 判断以下哪些可以作为字典的 key，哪些不行：
# 1）'name'（字符串）
# 2）100（数字）
# 3）('a', 'b')（元组）
# 4）['a', 'b']（列表）
# 请用代码验证
list1 = ['a','b']
a = 'name'
b = 100
c =('a','b',100)
# test1 = {list1:4} 不可哈希，除了4其他都可以
test2 = {a:1}
test3 = {b:2}
test4 = {c:3}

print('--------------------------- 练习 5 ---------------------------')
# 练习 5：
# 请定义一个嵌套字典，表示两个学生的信息
# 每个学生包含：姓名、年龄、成绩

students = {
'stu1':{'name':'甲','age':18,'score':88},
'stu2':{'name':'乙','age':12,'score':90}
            }
print(students)

print('--------------------------- 练习 6 ---------------------------')

# 练习 6（综合题）：
# 请定义一个字典，表示一个图书馆的藏书信息
# 要求：
# 1）以图书编号为 key
# 2）每本书包含：书名、作者、价格
# 3）包含至少 3 本书

information ={
    'book_1':{'书名':'《钢铁是怎样炼成的》','作者':'尼古拉·奥斯特洛夫斯基','价格':'50'},
    'book_2':{'书名':'《战争与和平》','作者':'列夫·托尔斯泰','价格':'104'},
    'book_3':{'书名':'《活着》','作者':'余华','价格':'63'}
}
print(information)